# Laporan Bug (Bug Report)

Dokumen ini berisi daftar temuan bug selama proses Tahap 4 (Black Box Execution).

| Bug ID | Date | Feature / Test ID | Severity | Description | Steps to Reproduce | Error Log / Evidence | Status |
|---|---|---|---|---|---|---|---|
| BUG-001 | 2026-07-02 | Pemesanan (TS-USR-02) | Critical | Terjadi `QueryException` karena kolom `nama_pemesan` tidak ditemukan saat *submit* form pemesanan ke database. Ini menyebabkan alur utama bisnis aplikasi terhenti total (500 Server Error). | 1. Login sebagai User<br>2. Ke halaman Katalog<br>3. Pilih barang & isi form tanggal<br>4. Submit Pesanan | `SQLSTATE[42S22]: Column not found: 1054 Unknown column 'nama_pemesan' in 'field list'` di `app\Http\Controllers\User\PemesananController.php:222` | Fixed |
| BUG-002 | 2026-07-03 | Pembayaran (TS-USR-06) | Critical | Terjadi `QueryException` karena kolom `snap_token` tidak ditemukan saat mencoba update status pembayaran Midtrans. | 1. Buka Detail Pesanan (status Menunggu DP)<br>2. Klik Bayar via Midtrans | `SQLSTATE[42S22]: Column not found: 1054 Unknown column 'snap_token' in 'field list'` | Fixed |
| BUG-003 | 2026-07-03 | Pembatalan (TS-USR-07) | Major | Tombol "Batalkan Pesanan" tidak muncul untuk pesanan dengan status "Menunggu Konfirmasi". | 1. Buat pesanan baru (status menunggu).<br>2. Buka detail pesanan tersebut. | Tombol tidak ter-render di UI. | Fixed |
| BUG-004 | 2026-07-03 | Penolakan Admin (TS-ADM-05) | Major | Tombol "Batalkan Pesanan" di panel admin tidak memunculkan prompt alasan penolakan dan form action mengarah ke rute pembatalan tanpa argumen penolakan. | 1. Buka pesanan status "Menunggu Konfirmasi" di Admin.<br>2. Cari tombol Tolak Pesanan / Batalkan Pesanan. | UI tidak mendukung rute `tolak` dari controller. | Fixed |
| BUG-005 | 2026-07-03 | Pembatalan (TS-USR-07) | Major | User tidak bisa membatalkan pesanannya sendiri saat statusnya masih "Menunggu Konfirmasi", tombol pembatalan tidak ada di halaman User. | 1. Pesan layanan hingga status "Menunggu Konfirmasi".<br>2. Buka detail pesanan di sisi User. | Status `menunggu` terlewat dari array `$bisaBatal` di view user. | Fixed |
| BUG-006 | 2026-07-03 | Pembatalan (TS-USR-07) | Minor | Tombol "Kirim Permintaan Pembatalan" di sisi User terlihat tidak berfungsi (tidak bisa diklik/submit form). | 1. Buka form Ajukan Pembatalan.<br>2. Isi dengan teks pendek (contoh: "Ubah tanggal").<br>3. Klik Kirim. | Konflik antara native `confirm()` pop-up dan HTML5 `minlength` validation, membuat interaksi terblokir tanpa peringatan jelas. | Fixed |
| BUG-007 | 2026-07-03 | Penolakan Admin (TS-ADM-05) | Minor | Tombol "Batalkan Pesanan" di sisi Admin tidak memicu aksi apapun ketika diklik. | 1. Buka pesanan status "Menunggu DP" di Admin.<br>2. Klik Batalkan Pesanan. | Browser memblokir fungsi `window.confirm()` native pada handler form, menyebabkan form diam. | Fixed |
| BUG-008 | 2026-07-04 | Pembatalan (TS-USR-07) | Major | "Pembatalan tidak dapat dilakukan pada status pesanan ini." muncul saat user mengajukan batal pada pesanan berstatus "menunggu". | 1. Login user.<br>2. Klik "Ajukan Pembatalan" pada pesanan status menunggu. | Backend (Controller) belum mengizinkan status 'menunggu' dalam array `$allowedStatuses`. | Fixed |
| BUG-009 | 2026-07-04 | Pembayaran (TS-USR-06) | Critical | Pembayaran Midtrans berhasil, namun status pesanan tetap `menunggu_dp` (tidak berubah ke `berlangsung`). | 1. Selesaikan pembayaran via Midtrans Snap.<br>2. Cek status di riwayat pesanan. | Membutuhkan investigasi lebih lanjut (Webhook / Notifikasi Midtrans tidak tersampaikan ke lokal). | Fixed |
| BUG-010 | 2026-07-04 | Riwayat Pesanan (TS-USR-04) | Major | Filter "Menunggu Pembayaran" di halaman User tidak menampilkan pesanan dengan status `menunggu_dp`. | 1. Buka halaman Riwayat Pesanan User.<br>2. Klik filter "Menunggu Pembayaran". | Query status pada filter backend/frontend tidak memetakan status `menunggu_dp` dengan tepat. | Fixed |
| BUG-011 | 2026-07-04 | Sirkulasi (TS-ADM-06/07) | Major | Tombol "Barang Sudah Dikembalikan" pada halaman detail pesanan Admin tidak berfungsi. | 1. Buka pesanan status sedang disewa di Admin.<br>2. Klik "Barang Sudah Dikembalikan". | Browser memblokir native `confirm()`. | Fixed |
| BUG-012 | 2026-07-04 | Pengembalian (TS-ADM-07) | Major | Controller pada halaman pengembalian tidak berfungsi. | 1. Akses menu Pengembalian.<br>2. Coba proses pengembalian. | Kegagalan validasi `denda_kerusakan` karena nilai kosong (null). Diperbaiki dengan default 0. | Fixed |
| BUG-013 | 2026-07-05 | Pembatalan (TS-ADM-09) | Major | Menu Kelola Pembatalan tidak muncul di sidebar admin, dan pengajuan pembatalan tidak merubah aksi/status di halaman detail pemesanan, sehingga admin kebingungan. | 1. Ajukan pembatalan dari User.<br>2. Buka panel Admin.<br>3. Buka detail Pemesanan dari pesanan terkait. | Rute menu belum dimasukkan ke view sidebar. Diperbaiki dengan menambah menu serta peringatan pembatalan pada halaman detail pesanan. | Fixed |
| BUG-014 | 2026-07-05 | Filter Pemesanan & Pengembalian | Minor | Filter status tidak berfungsi semestinya. Pesanan berstatus spesifik (seperti menunggu_dp) hilang dari filter, dan filter di halaman pengembalian bersifat dummy (tidak beroperasi di controller). | 1. Buka halaman Pemesanan Admin.<br>2. Klik tab filter "Menunggu Pembayaran".<br>3. Buka halaman Pengembalian. | Filter di PemesananController gagal me-mapping sub-status. Filter di PengembalianBarangController belum diimplementasikan backend-nya. | Fixed |
| BUG-015 | 2026-07-05 | Pengembalian | Minor | ID pesanan (RTR) di halaman list Pengembalian Barang terlihat rusak/hilang. | 1. Buka halaman Pengembalian.<br>2. Lihat list pengembalian. | Pemanggilan variabel fiktif `$return['kode']` dan `$return->kode_pengembalian` di file blade. | Fixed |
